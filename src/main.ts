import "source-map-support/register.js";
import { version } from "./version";

// TODO: Start the client, handle events, dispatch out to handler functions, etc.

console.debug(`Hello, world! Version ${version} has run.`);
